/target
/pkg
/dist

# Visual regression test baselines
# Note: Initial baseline images should be committed to version control
# Failing test screenshots (ending in _fail.png) should not be committed
tests/baselines/*_fail.png
